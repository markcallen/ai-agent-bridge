{
  "name": "chat-ts",
  "version": "0.1.0",
  "description": "TypeScript interactive chat example for ai-agent-bridge",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "test": "node --import tsx --test src/**/*.test.ts",
    "test:coverage": "c8 --reporter=text --reporter=lcov --report-dir coverage node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@ai-agent-bridge/client-node": "file:../../packages/bridge-client-node",
    "@grpc/grpc-js": "^1.11.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "c8": "^10.1.3",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
