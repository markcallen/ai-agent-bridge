#!/bin/sh
set -eu

if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
  systemctl stop ai-agent-bridge.service >/dev/null 2>&1 || true
  systemctl disable ai-agent-bridge.service >/dev/null 2>&1 || true
fi
